extends layout.pug

block content
     h1 New Book
     form
         p
             label(
                 for="title"
             ) 
             input(
                 name="title"
                 type="text"
                 id="title"
             )   
         p
             label(
                 for="author"
             )
             input(
                 name="author"
                 type="text"
                 id="author"
             )
         p
             label(
                 for="genre"
             ) 
             input(
                 name="genre"
                 type="text"
                 id="genre"
             )
         p
             label(
                 for="year"
             ) 
             input(
                 name="year"
                 type="text"
                 id="year"
             )   
         p
             input(
                 type="submit"
                 value="Create New Book"
             ) 
         p
            a(class="button" href="all_books.html") Cancel